# conference-infra

## YAML manifests and Helm chart for deploying the conference application in a Kubernetes cluster

1- Déploiement de metallb & ingress-nginx sur staging et prod avec argocd
  metallb/ application-staging.yaml & application-prod.yaml
  ingress-nginx/ application-staging.yaml & application-prod.yaml

